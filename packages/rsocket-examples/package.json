{
  "name": "@rsocket/rsocket-examples",
  "version": "0.1.0",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build",
    "start-request-response": "ts-node -r tsconfig-paths/register src/RequestResponseExample.ts"
  },
  "dependencies": {
    "ws": "~8.0.0",
    "@rsocket/rsocket-core": "^1.0.0",
    "@rsocket/rsocket-types": "^1.0.0",
    "@rsocket/rsocket-websocket-client": "^1.0.0"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "typescript": "~4.3.0",
    "ts-node": "~10.1.0",
    "tsconfig-paths": "~3.10.1"
  }
}